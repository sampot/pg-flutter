# 振翅穿林（`pg-flutter`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-flutter.yaml`

## 1. 一句話

單鍵振翅讓小鳥穿越樹幹間隙、每過一門加一分的一擊即死街機小品；致敬 flappy 類型玩法，非任一商業作品復刻。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-flutter` / `game` / `街機` |
| status | `listed` |
| 模式 | 單人無盡；一擊即死，死亡點一下重開 |
| 勝負 | 無終局條件，比「連續過門數」 |
| 視角/畫布 | 側向卷軸 320×360 邏輯畫布 |
| 物理 | 幀率綁定（每幀重力 +0.32、振翅 vy=−5.2、門速 −2.2/幀） |
| 素材 | 全部程式繪製；無音效 |
| 交付形 | 純 HTML＋CSS＋ESM JS；無 build；`npx --yes vitest run` 測試 |

## 3. 完整規則（現行實作）

### 3.1 目標與流程

- 小鳥固定在 x=80，只受重力與振翅影響；撞樹幹、飛出上緣（y<0）或落地（y>340）即結束。
- 分數＝成功穿過的樹門數（`passedGate`：門右緣越過鳥 x 且未記分）；死亡後 HUD 顯示分數，任意振翅輸入重開（分數歸零，**無任何最高分記錄**）。

### 3.2 參數與生成（實際值）

| 參數 | 值 |
| --- | --- |
| 鳥 | x=80、碰撞半徑 r=12（繪製橢圓 17×12 略大於判定圈）、傾角 `vy×0.04` |
| 振翅 / 重力 | vy=−5.2 ／ +0.32 每幀（非 dt 制，高刷螢幕會變快） |
| 樹門 | 寬 48；首門 x=300、gapY=150、gap=105 |
| 生成規則 | 最後一門 x<150 時補新門於 x=330；gapY=90+random×160、gap=100 |
| 卷軸速度 | 每幀 −2.2；x<−60 的門移除 |

### 3.3 判定與邊界

- 碰撞：鳥 x±r 與門水平重疊，且 y−r 低於上缺口緣或 y+r 高於下缺口緣 → 死亡。
- 上緣不反彈直接判死；地面 y>340 同樣判死（畫布高 360）。
- 死亡當幀停止物理更新但保留最後畫面；HUD 文案切換為「撞到了｜分數 N｜點一下重來」。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 點畫布／「振翅」鈕／空白鍵 | 振翅上升；死亡狀態下等同重開 |
| — | 無選單、無暫停、無靜音（本作無音效） |

- HUD 一行文字：進行中「連續過門 N」；死亡顯示分數。鳥身黃、嘴紅、樹幹深綠、背景青綠。
- Mobile-first：直向單欄；禁 `alert`／`confirm`／`prompt`。index.html 內藏未用的「模式：對 AI」按鈕（`hidden`，屬 scaffold 殘留）。

## 5. 持久化（KV 權威）

| key | 內容 |
| --- | --- |
| （無） | 本 repo 完全未用 localStorage／`/api/kv`；分數僅存頁面變數，重載即失 |

- functions.js 小節：僅健康檢查 stub——`GET /api/health` 回 `{ok:true}`，其餘路徑 404。未來加最高分時應走 `/api/kv/pg-flutter-best`（帶前綴），localStorage 只能快取。

## 6. 美術／音效／署名

- 無外部圖像資產：鳥、樹幹、背景全由 canvas 程序繪製；`ATTRIBUTION.md` 聲明全部介面美術為本作自繪（2026 Sampot），無第三方素材。
- **無音效**（無 audio.js；AudioContext 完全未用）。
- 新增素材：拷進 `assets/`、更新 `ATTRIBUTION.md`（CC0 也須署名）、同步 `sam-manifest.json` files。

## 7. 測試（`npx --yes vitest run`）

現有覆蓋（`game.test.js`，4 例）：振翅使 vy 變負向上、重力使 vy 遞增、樹幹碰撞判定命中、過門計分成立（`vitest.config.js` 限定 node 環境、只收 `game.test.js`）。

- 缺口：死亡邊界（y<0／y>340）、門生成間距、gapY/gap 隨機範圍皆無測試。最小必測：鳥在缺口內 `hitsGate=false`、同 x 在上下樹幹區 `hitsGate=true`、`passedGate` 已記分門不再重複計分。
- UI（app.js DOM）不在測試範圍。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；確認一律頁內 UI。
3. Mobile-first；主操作不可 hover-only（現行全畫面可點，符合）。
4. 未來新增任何分數/進度一律走 `/api/kv/{key}` 且帶 `pg-flutter-` 前綴；禁止裸 `localStorage` 當權威。
5. 不自行載入 `sdk.js`；宿主注入 `window.PG`。vanilla 即可，勿引入 Phaser。
6. 改動可執行邏輯前先寫失敗測試（TDD）。
7. 檔案清單變動須同步 `sam-manifest.json`。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：強化手感與重玩誘因，不改變「單鍵振翅」的核心認同。

**高優先**

1. **最高分持久化**：README 承諾「刷新紀錄」但實作完全沒有紀錄。加 session best＋`/api/kv/pg-flutter-best`（LS 快取），死亡畫面顯示「最佳 N」——最低成本的重玩誘因。
2. **dt 化物理**：改為以 dt 縮放（重力 ×dt/16.7ms），消除高刷新率裝置變難的不公平；`stepBird` 加 dt 參數並補 60/120fps 一致性測試。
3. **音效**：振翅短音、過門上行兩音、撞擊低音（WebAudio 合成即可，參考 pg-breakout `audio.js` 寫法）；節奏遊戲感的類型沒有聲音回饋是最大缺口。

**中優先**

4. **難度曲線**：過 10 門後卷軸速漸增（如每 10 門 +0.15，上限 3.4）或 gap 由 100 收窄至 84，讓高手有終點壓力。
5. **視覺回饋**：過門瞬間門緣閃光＋分數跳動；死亡慢動作定格 300ms 再浮出成績，強化情緒峰值。
6. **里程碑 toast**：10/25/50 門顯示稱號，KV 存生涯最高稱號等級。

**低優先**

7. 雲朵／灌木前景視差層（兩層不同速度），零成本增加速度感。
8. 振翅次數統計（KV JSON）：平均每門振翅數之類的趣味數據。
